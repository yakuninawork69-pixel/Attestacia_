#приветствие
print("Привет, мир! 🌍")

# сумма двух чисел
num1 = float(input("Введите первое число: "))
num2 = float(input("Введите второе число: "))
result = num1 + num2
print("Сумма:", result)
# Пояснение:
# input() запрашивает ввод пользователя (возвращает строку).
# float() преобразует строку в число с плавающей запятой (для целых чисел можно использовать int).
# Результат сложения сохраняется в переменной result.
# print() выводит ответ в формате: Сумма: <результат>.

# Проверка числа на чётность
number = int(input("Введите число: "))
if number % 2 == 0:
    print(number, "чётное")
else:
    print(number, "нечётное")
# Пояснение:
# % — оператор остатка от деления.
# Если число делится на 2 без остатка (number % 2 == 0), оно чётное.
# if-else проверяет условие и выводит соответствующий результат

# Поиск максимума из трёх чисел
a = float(input("Введите первое число: "))
b = float(input("Введите второе число: "))
c = float(input("Введите третье число: "))
maximum = max(a, b, c)
print("Максимум:", maximum)
# Пояснение:
# Встроенная функция max() находит наибольшее значение среди аргументов.
# Альтернатива без max — сравнение через условия:
if a >= b and a >= c:
    maximum = a
elif b >= a and b >= c:
    maximum = b
else:
    maximum = c

# Подсчёт суммы элементов списка
numbers = [5, 2, 9, 1, 7]
total = sum(numbers)
print("Сумма:", total)
# Пояснение:
# Список numbers содержит произвольные числа.
# sum() возвращает сумму всех элементов списка.
# Альтернатива с циклом:
total = 0
for num in numbers:
    total += num